# Login A11y Quick Check

- Keyboard navigation: tab order reaches email, password, submit button, and links.
- Focus visibility: inputs and primary button show visible focus ring.
- Error semantics: invalid fields expose `aria-invalid` and inline error text.
- Status semantics: successful login feedback uses `output` with `aria-live`.
- Labels: both fields expose visible labels matching expected context.
